// tailwind.config.mjs
// DOCUMENTATION ONLY — not read by the build (Tailwind v4 uses CSS-based config).
// Design tokens are defined in src/styles/global.css via @theme.
//
// Design system reference:
//   navy-deep  #0d2233  (dark background)
//   navy       #1a3a4a  (primary background)
//   navy-mid   #1f5068  (secondary background)
//   teal       #7ecdc8  (accent / CTA)
//   light-bg   #f7fafc  (light section background)
//   muted      #667788  (secondary text on light bg)
//   Font: Montserrat 300 (body) / 800-900 (headings)
